<p>Dev</p>
<ul class="links">
    {{#get "posts" filter="tag:menu" }}
        {{#foreach featured_post}}
        <li class="nav-{{slug}} {{#if current}}active{{/if}}">
            <a href="{{url absolute="true"}}">{{label}}</a>
        </li>
        {{/foreach}}
    {{/get}}
</ul>
